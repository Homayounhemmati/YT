{
  "name": "lifecalc-pro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "data:extract": "python3 scripts/extract_tax_data.py --year 2026 && python3 scripts/estimated_tax.py --year 2026",
    "data:validate": "python3 scripts/validate_tax_data.py --year 2026"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
